import { createTheme } from '@mui/material/styles';

const globalTheme = createTheme({
  palette: {
    primary: {
      main: "#009688"
    },
    secondary: {
      main: "#ff6977"
    }
  }
});

export default globalTheme;
